'use client';

// Simple layout for forgot password page that doesn't require authentication
export default function ForgotPasswordLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}












