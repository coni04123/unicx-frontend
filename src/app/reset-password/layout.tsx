'use client';

// Simple layout for reset password page that doesn't require authentication
export default function ResetPasswordLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}












